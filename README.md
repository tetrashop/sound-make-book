# 🎧 sound-make-book

**تبدیل متن به کتاب صوتی با موتور eSpeak (بدون نیاز به اینترنت و API Key)**

این پروژه یک وب‌اپلیکیشن ساده و کارآمد است که متن وارد شده توسط کاربر را به فایل صوتی تبدیل می‌کند. موتور تبدیل متن به صدا بر پایه **eSpeak** (یک موتور متن‌باز و آفلاین) کار می‌کند و برای زبان فارسی به خوبی عمل می‌کند. نیازی به API Key یا اتصال به اینترنت ندارد و کاملاً مستقل اجرا می‌شود.

---

## ✨ ویژگی‌ها

- ✅ **تبدل متن به صدا** با کیفیت قابل قبول (مناسب برای کتاب‌های صوتی)
- ✅ **پشتیبانی کامل از زبان فارسی** (و سایر زبان‌های پشتیبانی‌شده توسط eSpeak)
- ✅ **بدون نیاز به API Key** و کاملاً آفلاین
- ✅ **رابط کاربری ساده و واکنش‌گرا** (RTL)
- ✅ **قابل اجرا در Termux** (اندروید) و سرورهای لینوکسی
- ✅ **استقرار آسان** روی سرویس‌های ابری مانند Vercel، Railway و Render
- ✅ **امکان تنظیم سرعت و زیروبمی صدا** (در نسخه‌های بعدی)

---

## 🛠️ تکنولوژی‌های استفاده‌شده

- **بک‌اند:** Node.js + Express
- **موتور TTS:** eSpeak (نصب شده روی سیستم)
- **فرانت‌اند:** HTML5، CSS3، JavaScript (ساده)
- **پردازش صدا:** ffmpeg (برای تبدیل WAV به MP3)
- **مدیریت وابستگی‌ها:** npm

---

## 📁 ساختار پروژه

```

sound-make-book/
├── api/
│   ├── index.js          # سرور اصلی
│   └── tts-espeak.js     # ماژول تبدیل متن به صدا با eSpeak
├── public/
│   └── index.html        # رابط کاربری وب
├── audio-cache/          # فایل‌های صوتی موقت
├── .env.example          # نمونه فایل متغیرهای محیطی (اختیاری)
├── .gitignore
├── package.json
└── README.md

```

---

## 🚀 نحوه نصب و اجرا (محلی)

### پیش‌نیازها

- **Node.js** نسخه ۱۴ یا بالاتر
- **eSpeak** (برای تبدیل متن به صدا)
- **ffmpeg** (اختیاری، برای تبدیل فایل‌های WAV به MP3)

### نصب در Termux (اندروید)

```bash
# به‌روزرسانی مخازن و نصب بسته‌های مورد نیاز
pkg update && pkg upgrade -y
pkg install nodejs espeak ffmpeg -y

# کلون پروژه
git clone https://github.com/tetrashop/sound-make-book.git
cd sound-make-book

# نصب وابستگی‌های Node.js
npm install

# اجرای سرور
node api/index.js
```

سپس در مرورگر موبایل آدرس http://localhost:3000 را باز کنید.

نصب در لینوکس (اوبونتو/دبیان)

```bash
sudo apt update
sudo apt install nodejs npm espeak ffmpeg -y

git clone https://github.com/tetrashop/sound-make-book.git
cd sound-make-book
npm install
node api/index.js
```

---

🌐 استقرار در فضای ابری

گزینه ۱: استقرار روی Vercel (در صورت دسترسی)

```bash
npm install -g vercel
vercel login          # (با VPN وارد شوید)
vercel --prod
```

نکته: به دلیل تحریم، ممکن است Vercel در ایران در دسترس نباشد. از VPN استفاده کنید یا گزینه‌های جایگزین را امتحان کنید.

گزینه ۲: استقرار روی Railway (پیشنهادی)

```bash
npm install -g @railway/cli
railway login
railway up
```

یا از طریق سایت railway.app و اتصال به گیت‌هاب.

گزینه ۳: استقرار روی Render

1. در render.com ثبت‌نام کنید (با VPN).
2. مخزن گیت‌هاب خود را متصل کنید.
3. یک Web Service جدید با فرمان node api/index.js ایجاد کنید.
4. پس از استقرار، آدرس سرویس شما فعال خواهد شد.

---

📝 به‌روزرسانی مخزن گیت‌هاب

پس از هر تغییر در پروژه، مراحل زیر را برای ارسال به گیت‌هاب انجام دهید:

```bash
git add .
git commit -m "توضیح مختصر تغییرات"
git push origin main
```

مخزن عمومی در آدرس زیر قابل مشاهده است:
🔗 https://github.com/tetrashop/sound-make-book

---

🧪 تست و عیب‌یابی

· تست eSpeak: در ترمینال اجرا کنید:
  ```bash
  espeak -v fa "سلام این یک تست است"
  ```
  اگر صدا شنیده نشد، eSpeak به درستی نصب نشده است.
· بررسی لاگ سرور: پس از اجرا، پیام 🚀 sound-make-book server running on port 3000 باید نمایش داده شود.
· مشکل در تولید فایل صوتی: مطمئن شوید پوشه audio-cache قابل نوشتن است (chmod 755 audio-cache).
· خطای ECONNRESET در Vercel: نشانه قطع ارتباط با سرور است. از VPN پایدار استفاده کنید.

---

🤝 مشارکت در توسعه

اگر ایده یا بهبودی دارید، خوشحال می‌شویم:

· Issue ثبت کنید
· Pull Request بفرستید
· پروژه را فورک کرده و تغییرات خود را اعمال کنید

---

📜 مجوز

این پروژه تحت مجوز MIT منتشر شده است. آزادانه استفاده کنید، تغییر دهید و به اشتراک بگذارید.

---

❤️ سازنده

ساخته شده با عشق توسط tetrashop
گیت‌هاب | پروژه‌های دیگر

---

تاریخ آخرین به‌روزرسانی: فوریه ۲۰۲۶

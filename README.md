# 🎧 sound-make-book

**تبدیل متن به کتاب صوتی با ۹ قابلیت پیشرفته**  
توسعه‌دهنده: **رامین اجلال** ([@tetrashop](https://github.com/tetrashop))  
مخزن گیت‌هاب: [https://github.com/tetrashop/sound-make-book](https://github.com/tetrashop/sound-make-book)  
نسخه آنلاین (معرفی): [https://sound-make-book.vercel.app](https://sound-make-book.vercel.app)

---

## 📋 فهرست مطالب

- [معرفی پروژه](#معرفی-پروژه)
- [قابلیت‌های پروژه (۹ قابلیت)](#قابلیت‌های-پروژه-۹-قابلیت)
- [وضعیت توسعه](#وضعیت-توسعه)
- [معماری فنی](#معماری-فنی)
- [نیازمندی‌ها](#نیازمندی‌ها)
- [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
  - [اجرای محلی (با قابلیت‌های کامل)](#اجرای-محلی-با-قابلیت‌های-کامل)
  - [استقرار روی Vercel (نسخه معرفی)](#استقرار-روی-vercel-نسخه-معرفی)
  - [نسخه اندروید (مستقل)](#نسخه-اندروید-مستقل)
- [ساختار پروژه](#ساختار-پروژه)
- [مستندات API](#مستندات-api)
- [عیب‌یابی مشکلات رایج](#عیب‌یابی-مشکلات-رایج)
- [رفع مشکل TTS فارسی در اندروید](#رفع-مشکل-tts-فارسی-در-اندروید)
- [توسعه و مشارکت](#توسعه-و-مشارکت)
- [مجوز](#مجوز)

---

## 🎯 معرفی پروژه

**sound-make-book** یک پلتفرم چندسکویی (وب، دسکتاپ، اندروید) برای تبدیل خودکار متن به کتاب صوتی است. این پروژه با هدف ارائه راه‌حلی آفلاین، رایگان و فارسی برای تولید کتاب‌های صوتی توسعه یافته و از ۹ قابلیت پیشرفته پشتیبانی می‌کند.

پروژه در سه نسخه قابل استفاده است:

1. **نسخه وب (محلی)**: با سرور Node.js و موتور eSpeak (تمام قابلیت‌ها)
2. **نسخه آنلاین (معرفی)**: مستقر روی Vercel برای نمایش پیام راهنما
3. **نسخه اندروید (مستقل)**: اپلیکیشن بومی با TTS داخلی اندروید

---

## ✨ قابلیت‌های پروژه (۹ قابلیت)

| # | قابلیت | توضیح | وضعیت در نسخه‌های مختلف |
|---|--------|-------|------------------------|
| ۱ | **تشخیص خودکار زبان** | تشخیص هوشمند زبان متن (فارسی/انگلیسی) | ✅ وب / ✅ اندروید |
| ۲ | **تنظیم چندصدایی** | تقسیم متن به بخش‌ها با صداهای متفاوت | ✅ وب / ⚠️ اندروید* |
| ۳ | **موسیقی پس‌زمینه** | اضافه کردن موسیقی ملایم با کنترل حجم | ✅ وب / ❌ اندروید |
| ۴ | **OCR (تشخیص متن از تصویر)** | استخراج متن از عکس‌ها و اسناد اسکن‌شده | ✅ وب / ❌ اندروید |
| ۵ | **پیش‌نمایش صدا** | پخش ۱۰ ثانیه اول قبل از تولید نهایی | ✅ وب / ✅ اندروید |
| ۶ | **تنظیمات پیشرفته صدا** | کنترل سرعت، زیروبمی و فاصله کلمات | ✅ وب / ✅ اندروید |
| ۷ | **داشبورد پروژه‌ها** | ذخیره، مدیریت و دانلود مجدد کتاب‌های صوتی | ✅ وب / ✅ اندروید |
| ۸ | **پشتیبانی از فایل‌ها** | آپلود متن از فایل‌های TXT | ✅ وب / ❌ اندروید |
| ۹ | **ذخیره در حافظه** | ذخیره فایل‌های صوتی در حافظه داخلی | ✅ وب / ✅ اندروید |

> ⚠️ *قابلیت چندصدایی در نسخه اندروید نیاز به توسعه بیشتر دارد.

---

## 🚀 وضعیت توسعه

| نسخه | وضعیت | دسترسی | توضیحات |
|------|--------|--------|---------|
| **نسخه وب (محلی)** | ✅ پایدار | [مخزن اصلی](https://github.com/tetrashop/sound-make-book) | تمام ۹ قابلیت با سرور Node.js |
| **نسخه آنلاین** | ✅ فعال (معرفی) | [sound-make-book.vercel.app](https://sound-make-book.vercel.app) | فقط صفحه معرفی (به دلیل محدودیت Serverless) |
| **نسخه اندروید** | ✅ پایدار | [Releases](https://github.com/tetrashop/sound-make-book/releases/latest) | APK مستقل با ۵ قابلیت اصلی |

---

## 🧠 معماری فنی

### **نسخه محلی (کامل)**
- **زبان**: Node.js (بدون فریم‌ورک، با ماژول‌های داخلی http, fs, path)
- **موتور تبدیل متن به صدا**: eSpeak (نصب جداگانه روی سیستم)
- **پردازش صدا**: ffmpeg (تبدیل WAV به MP3)
- **فرانت‌اند**: HTML5, CSS3, JavaScript (ساده)
- **ذخیره‌سازی پروژه‌ها**: فایل JSON در پوشه projects/
- **فایل‌های صوتی**: ذخیره در پوشه audio-cache/

### **نسخه آنلاین (معرفی)**
- **پلتفرم**: Vercel (Serverless Functions)
- **محدودیت‌ها**: عدم دسترسی به eSpeak و ffmpeg → فقط پیام راهنما نمایش داده می‌شود.
- **هدف**: معرفی پروژه و هدایت کاربران به اجرای محلی.

### **نسخه اندروید (مستقل)**
- **زبان**: Java (با WebViewAssetLoader)
- **موتور تبدیل متن به صدا**: TextToSpeech بومی اندروید
- **ذخیره‌سازی پروژه‌ها**: localStorage در WebView

---

## 📦 نیازمندی‌ها

### برای اجرای محلی (با قابلیت‌های کامل)
- **Node.js** ≥ 14
- **eSpeak** (برای تبدیل متن به صدا)
- **ffmpeg** (اختیاری، برای تبدیل به MP3)
- **سیستم‌عامل**: لینوکس، Termux (اندروید)، macOS

### برای استقرار روی Vercel
- حساب Vercel
- مخزن گیت‌هاب

---

## 🚀 نصب و راه‌اندازی

### اجرای محلی (با قابلیت‌های کامل)

#### **۱. نصب پیش‌نیازها**

**در Termux (اندروید):**
```bash
pkg update && pkg upgrade -y
pkg install nodejs espeak ffmpeg git -y
```

در اوبونتو/دبیان:

```bash
sudo apt update
sudo apt install nodejs npm espeak ffmpeg git -y
```

۲. کلون کردن مخزن

```bash
git clone https://github.com/tetrashop/sound-make-book.git
cd sound-make-book
```

۳. اجرای سرور

```bash
node api/index.js
```

۴. باز کردن در مرورگر

```bash
termux-open-url http://localhost:3000   # در Termux
# یا دستی: http://localhost:3000
```

استقرار روی Vercel (نسخه معرفی)

```bash
# نصب Vercel CLI
npm install -g vercel

# ورود به حساب (ممکن است نیاز به VPN باشد)
vercel login

# استقرار
vercel --prod
```

یا از طریق وب‌سایت Vercel (با اتصال به گیت‌هاب).

نسخه اندروید (مستقل)

آخرین نسخه APK را از بخش Releases دانلود و نصب کنید:

```bash
https://github.com/tetrashop/sound-make-book/releases/latest
```

---

📁 ساختار پروژه

```
sound-make-book/
├── api/
│   └── index.js                 # سرور اصلی (پشتیبانی از هر دو محیط)
├── public/
│   ├── index.html                # صفحه اصلی (۵ تب)
│   ├── styles/
│   │   └── main.css              # استایل‌ها
│   ├── scripts/
│   │   └── main.js               # منطق فرانت‌اند
│   └── music/                     # فایل‌های موسیقی پس‌زمینه
│       ├── soft_piano.mp3
│       └── cinematic.mp3
├── audio-cache/                   # فایل‌های صوتی موقت
├── projects/                       # ذخیره پروژه‌ها (JSON)
│   └── projects.json
├── android-app/                    # پروژه اندروید
│   └── app/
│       ├── src/
│       └── build.gradle
├── .gitignore
├── package.json
├── vercel.json
└── README.md
```

---

🌐 مستندات API

Method Endpoint توضیح نمونه درخواست
GET /api/health بررسی سلامت سرور -
POST /api/detect-language تشخیص زبان متن {"text":"سلام"}
POST /api/ocr استخراج متن از تصویر (multipart/form-data)
POST /api/preview پیش‌نمایش ۱۰ ثانیه {"text":"...", "voice":"fa"}
POST /api/generate-multi-voice تولید با چند صدا {"segments":[{"text":"...","voice":"fa"}]}
POST /api/generate-advanced تولید با تنظیمات پیشرفته {"text":"...","speed":1.2,"pitch":5}
GET /api/projects لیست پروژه‌ها -
POST /api/projects ذخیره پروژه جدید {"name":"...","audioUrl":"..."}
DELETE /api/projects/:id حذف پروژه -

توجه: در نسخه آنلاین، همه endpointها به جز / و /api/health خطای ۴۰۴ برمی‌گردانند.

---

🔧 عیب‌یابی مشکلات رایج

۱. خطای EADDRINUSE: address already in use

علت: پورت ۳۰۰۰ توسط برنامه دیگری اشغال شده است.
راه‌حل:

```bash
lsof -i :3000   # پیدا کردن PID
kill -9 <PID>   # کشتن فرآیند
# یا تغییر پورت در فایل api/index.js
```

۲. خطای espeak: command not found

علت: eSpeak نصب نیست.
راه‌حل: pkg install espeak (در Termux) یا sudo apt install espeak (در لینوکس).

۳. خطای ۵۰۰ در Vercel

علت: تلاش برای اجرای eSpeak در محیط Serverless.
راه‌حل: از قبل پیش‌بینی شده و پروژه در Vercel فقط پیام راهنما نمایش می‌دهد. برای قابلیت‌های کامل، محلی اجرا کنید.

۴. فایل‌های CSS/JS لود نمی‌شوند

علت: مسیردهی اشتباه یا پوشه‌های styles و scripts وجود ندارند.
راه‌حل:

```bash
mkdir -p public/styles public/scripts
# و مطمئن شوید فایل‌ها در آنجا هستند
```

---

🗣️ رفع مشکل TTS فارسی در اندروید

اگر در نسخه اندروید با پیام «زبان فارسی پشتیبانی نمی‌شود» مواجه شدید، به این معناست که بسته‌ی صوتی زبان فارسی روی دستگاه شما نصب نیست. برای رفع این مشکل:

📥 دانلود و نصب بسته صوتی فارسی

1. مخزن رسمی بسته صوتی پروژه را باز کنید:
      tetrashop/tts-persian-packages
2. به بخش Releases بروید و آخرین نسخه (espeak-persian-data.tar.gz) را دانلود کنید.
3. فایل دانلود شده را در مسیر زیر در گوشی خود از حالت فشرده خارج کنید:
   ```
   /data/data/com.termux/files/usr/share/
   ```
   (برای این کار می‌توانید از یک برنامه مدیریت فایل مانند CX File Explorer استفاده کنید.)
4. پس از کپی کردن فایل‌ها، برنامه sound-make-book را مجدداً راه‌اندازی کنید. حالا موتور TTS قادر به تولید صدای فارسی خواهد بود.

نکته: این بسته صوتی بر پایه موتور متن‌باز eSpeak تهیه شده و کاملاً رایگان و آفلاین است.

---

🤝 توسعه و مشارکت

اگر ایده یا بهبودی دارید، خوشحال می‌شویم:

1. مخزن را Fork کنید.
2. یک شاخه جدید (git checkout -b feature/amazing-feature) ایجاد کنید.
3. تغییرات را Commit کنید (git commit -m 'Add amazing feature').
4. به شاخه اصلی Push کنید (git push origin feature/amazing-feature).
5. یک Pull Request باز کنید.

ایده‌های توسعه آینده

· اضافه کردن کتابخانه tesseract.js برای OCR واقعی
· اتصال به سرویس‌های TTS ابری (Google، OpenAI) برای کیفیت بهتر
· ذخیره‌سازی فایل‌ها در S3 یا فضای ابری برای نسخه آنلاین
· اضافه کردن قابلیت اشتراک‌گذاری پروژه‌ها

---

📜 مجوز

این پروژه تحت مجوز MIT منتشر شده است. آزادانه استفاده کنید، تغییر دهید و به اشتراک بگذارید.

---

👨‍💻 توسعه‌دهنده

رامین اجلال
گیت‌هاب | پروژه‌های دیگر

---

تاریخ آخرین به‌روزرسانی: بهمن ۱۴۰۴
نسخه: ۲.۰.۰
```
